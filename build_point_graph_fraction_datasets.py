"""DEPRECATED: fraction-of-cell subgraphs (slow, 1000+ nodes).

Use ``build_point_graph_size_datasets.py`` instead (shell cutoff_k sweep).
"""

raise SystemExit(
    "build_point_graph_fraction_datasets.py is deprecated. "
    "Use build_point_graph_size_datasets.py (shell cutoff_k tiers)."
)
